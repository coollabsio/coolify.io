# Site URL for sitemap generation
SITE_URL=https://coolify.io

# GitHub Personal Access Token
# Used by scripts/fetch-contributors.js to fetch contributor data
# Without this token, the script is limited to 60 requests/hour
# With a token, the limit increases to 5,000 requests/hour
#
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "Coolify Contributors Script"
# 4. Select scope: "public_repo" (read access to public repositories)
# 5. Click "Generate token"
# 6. Copy the token and add it below
#
# For local development:
# Copy this file to .env and add your token
#
# For CI/CD and production builds:
# Set GITHUB_TOKEN as an environment variable in your build system
GITHUB_TOKEN=your_github_token_here
